<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Редактирование</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            margin: 0;
            height: 100vh;
            background-color: #f4f4f9; /* светлый фон для страницы */
            font-family: 'Arial', sans-serif; /* Шрифт */
        }
        
        nav {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #007bff; /* синий фон */
            border-radius: 8px; /* скругленные углы */
            padding: 10px 20px; /* внутренние отступы */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* тень для добавления глубины */
        }

        nav a {
            color: white; /* белый текст */
            text-decoration: none; /* убирает подчеркивание */
            font-size: 16px;
            font-weight: bold;
            transition: all 0.3s ease; /* плавный переход для эффектов */
        }

        nav a:hover {
            color: #ffd700; /* золотистый цвет при наведении */
            text-decoration: underline; /* подчеркивание при наведении */
        }

        nav a:active {
            color: #ff4500; /* оранжевый цвет при нажатии */
        }

        .action-buttons {
            display: flex;
            justify-content: center; /* Центрирует кнопки по горизонтали */
            gap: 15px; /* промежуток между кнопками */
        }

        .action-buttons a {
            background-color: #28a745; /* зеленый фон */
            color: white; /* белый текст */
            text-decoration: none; /* убирает подчеркивание */
            padding: 12px 25px;
            border-radius: 8px;
            text-align: center;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .action-buttons a:hover {
            background-color: #218838; /* темно-зеленый при наведении */
        }

        .action-buttons a:active {
            background-color: #1e7e34; /* при нажатии */
        }

        .edit-container {
            max-width: 400px;
            margin: 50px auto;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .edit-container input {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
        }

        .edit-container button {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            color: white;
            font-size: 16px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .edit-container button:hover {
            background-color: #0056b3;
        }

        .edit-container button:active {
            background-color: #003f7f;
        }
    </style>
</head>
<body>
    <nav>
        <a href="/logout">Выйти</a>
    </nav>

    <div class="edit-container">
        <h1>Сменить логин</h1>
        <form action="/user/edit" method="POST" style="margin-top: 15px;">
            <input type="text" name="username" placeholder="Новый логин" required>
            <button type="submit">Изменить</button>
        </form>
        <div class="action-buttons" style="margin-top: 10px;">
            <a href="/user">Отмена</a>
        </div>
    </div>
</body>
</html>
